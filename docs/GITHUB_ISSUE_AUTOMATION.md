# GitHub 이슈 자동 생성 가이드

이 문서는 `tasks/` 폴더의 마크다운 파일을 기반으로 GitHub 이슈를 자동으로 생성하는 전체 프로세스를 설명합니다.

## 📋 개요

ReAcademix 프로젝트의 향후 개선 작업들을 `tasks/` 폴더에 마크다운 파일로 정리하고, 이를 GitHub 이슈로 자동 변환하여 프로젝트 관리와 추적을 용이하게 합니다.

## 🔄 전체 프로세스

### 1단계: 작업 목록 작성

#### 1.1 README.md 분석
- 프로젝트의 `README.md` 파일을 읽어 향후 개선 아이디어 리스트 확인
- 우선순위별로 작업 분류 (높음/중간/낮음)

#### 1.2 작업 파일 생성
`tasks/` 폴더 구조에 우선순위별로 작업 파일 생성:

```
tasks/
├── high-priority/      # 🔴 우선순위 높음 (1-2개월)
│   ├── 01-테스트-코드-추가.md
│   ├── 02-에러-처리-강화.md
│   └── 03-접근성-개선.md
├── medium-priority/    # 🟡 우선순위 중간 (2-4개월)
│   ├── 04-컴포넌트-분리-및-리팩토링.md
│   ├── 05-상태-관리-개선.md
│   ├── 06-API-호출-최적화.md
│   └── 07-Firebase-Firestore-연동.md
├── low-priority/       # 🟢 우선순위 낮음 (4개월 이후)
│   ├── 08-성능-최적화.md
│   ├── 09-검색-기능-강화.md
│   └── ... (총 15개 작업)
└── README.md
```

#### 1.3 작업 파일 형식
각 작업 파일은 다음 정보를 포함:

```markdown
# 작업 제목

## 우선순위: 🔴 높음
## 예상 시간: 2-3주

## 작업 내용

### 1. 작업 섹션
- [ ] 작업 항목 1
- [ ] 작업 항목 2

## 참고 사항
- 참고 자료 및 링크
```

### 2단계: GitHub 이슈 형식으로 변환

#### 2.1 이슈 템플릿 생성
`.github/ISSUE_TEMPLATE/` 폴더에 이슈 템플릿 생성:

- `task.md`: 작업 이슈 템플릿
- `bug_report.md`: 버그 리포트 템플릿

#### 2.2 GitHub 이슈 형식 파일 생성
`tasks/github-issues/` 폴더에 GitHub 이슈로 바로 사용 가능한 형식으로 변환:

```markdown
# [Task] 작업 제목

**라벨**: `task`, `high-priority`, `testing`  
**예상 시간**: 2-3주

---

## 📋 작업 개요
...

## 📝 작업 내용
- [ ] 작업 항목 1
- [ ] 작업 항목 2
...
```

**변환된 파일 특징:**
- 제목에 `[Task]` 접두사 추가
- 라벨 정보를 명시적으로 표시
- GitHub 이슈 본문으로 바로 사용 가능한 형식

### 3단계: 자동화 스크립트 작성

#### 3.1 스크립트 파일 생성
`scripts/` 폴더에 다음 스크립트 생성:

1. **`create-issues-from-tasks.js`** (Node.js - 권장)
   - 크로스 플랫폼 지원
   - 가장 안정적

2. **`create-issues-from-tasks.ps1`** (PowerShell)
   - Windows 네이티브 지원

3. **`create-issues-from-tasks.sh`** (Bash)
   - Linux/Mac/WSL 지원

#### 3.2 스크립트 주요 기능

**추출 기능:**
- 제목 추출: 마크다운 파일의 첫 번째 `# 제목` 또는 파일명에서 추출
- 본문 추출: 전체 마크다운 파일 내용
- 라벨 추출: `**라벨**:` 줄에서 자동 추출

**자동화 기능:**
- 중복 체크: 기존 이슈 제목과 비교하여 중복 생성 방지
- 라벨 자동 생성: 필요한 라벨이 없으면 자동으로 생성
- 'Issue Automation' 라벨 자동 추가
- 진행 상황 실시간 출력
- 결과 요약 제공

#### 3.3 package.json 스크립트 추가

```json
{
  "scripts": {
    "create-issues": "node scripts/create-issues-from-tasks.js"
  }
}
```

### 4단계: GitHub CLI 설정

#### 4.1 GitHub CLI 설치

**Windows:**
```bash
winget install --id GitHub.cli
```

**macOS:**
```bash
brew install gh
```

**Linux:**
```bash
sudo apt install gh
```

#### 4.2 GitHub CLI 인증

```bash
gh auth login
```

인증 옵션 선택:
- GitHub.com 선택
- HTTPS 프로토콜 선택
- 웹 브라우저로 인증 선택
- 일회용 코드 복사 후 브라우저에서 인증

### 5단계: 이슈 생성 실행

#### 5.1 스크립트 실행

```bash
npm run create-issues
```

또는

```bash
node scripts/create-issues-from-tasks.js
```

#### 5.2 실행 과정

1. **환경 확인**
   - GitHub CLI 설치 확인
   - GitHub CLI 인증 상태 확인
   - `tasks/github-issues/` 폴더 존재 확인

2. **파일 스캔**
   - `tasks/github-issues/` 폴더의 모든 `.md` 파일 검색
   - `README.md` 제외
   - 파일명 순서로 정렬

3. **기존 이슈 확인**
   - `gh issue list` 명령으로 기존 이슈 목록 가져오기
   - 제목을 소문자로 변환하여 중복 체크

4. **각 파일 처리**
   - 제목 추출
   - 본문 읽기
   - 라벨 추출
   - 중복 체크
   - 라벨 존재 확인 및 자동 생성
   - 이슈 생성

5. **결과 출력**
   - 생성/건너뜀/실패 개수 요약
   - 생성된 이슈 URL 표시

#### 5.3 실행 결과 예시

```
🚀 GitHub 이슈 자동 생성 시작

📁 15개의 작업 파일을 찾았습니다.

📋 기존 이슈 목록 확인 중...
   0개의 기존 이슈를 찾았습니다.

📄 처리 중: 01-테스트-코드-추가.md
   제목: 테스트 코드 추가
  🏷️  라벨 생성 중: task
  🏷️  라벨 생성 중: high-priority
  🏷️  라벨 생성 중: testing
  🏷️  라벨 생성 중: Issue Automation
  📝 이슈 생성 중: 테스트 코드 추가
   ✅ 이슈 생성 완료: https://github.com/.../issues/3

...

📊 결과 요약
   ✅ 생성됨: 15개
   ⏭️  건너뜀: 0개
   ❌ 실패: 0개
```

## 📁 생성된 파일 구조

```
프로젝트 루트/
├── .github/
│   └── ISSUE_TEMPLATE/
│       ├── task.md
│       └── bug_report.md
├── scripts/
│   ├── create-issues-from-tasks.js
│   ├── create-issues-from-tasks.ps1
│   ├── create-issues-from-tasks.sh
│   └── README.md
├── tasks/
│   ├── github-issues/
│   │   ├── 01-테스트-코드-추가.md
│   │   ├── 02-에러-처리-강화.md
│   │   └── ... (15개 파일)
│   ├── high-priority/
│   ├── medium-priority/
│   ├── low-priority/
│   └── README.md
└── package.json
```

## 🔧 스크립트 동작 원리

### 제목 추출 로직

```javascript
// 1. 마크다운 파일의 첫 번째 # 제목에서 추출
const titleMatch = content.match(/^#\s+(.+)$/m);
if (titleMatch) {
  return titleMatch[1].replace(/^\[Task\]\s*/, '').trim();
}

// 2. 파일명에서 추출 (대체 방법)
const fileName = path.basename(filePath, '.md');
return fileName.replace(/^\d+-/, '').replace(/-/g, ' ');
```

### 라벨 추출 로직

```javascript
// **라벨**: `task`, `high-priority`, `testing` 형식에서 추출
const labelMatch = content.match(/\*\*라벨\*\*:\s*(.+)/);
if (labelMatch) {
  const labels = labelMatch[1]
    .split(',')
    .map(l => l.trim().replace(/`/g, ''))
    .filter(l => l && l !== AUTOMATION_LABEL);
  return labels;
}
```

### 중복 체크 로직

```javascript
// 기존 이슈 목록 가져오기
const existingIssues = await getExistingIssues();

// 제목을 소문자로 변환하여 비교
const titleLower = title.toLowerCase();
if (existingIssues.has(titleLower)) {
  // 중복 발견, 건너뜀
  return;
}
```

### 라벨 자동 생성 로직

```javascript
// 라벨 존재 확인
if (!labelExists(label)) {
  // 라벨 생성
  createLabel(label);
}
```

## ✅ 체크리스트

작업을 수행하기 전 다음 사항을 확인하세요:

- [ ] GitHub CLI 설치됨 (`gh --version`)
- [ ] GitHub CLI 인증 완료 (`gh auth status`)
- [ ] 저장소에 이슈 생성 권한 있음
- [ ] `tasks/github-issues/` 폴더에 작업 파일 존재
- [ ] 작업 파일이 올바른 형식으로 작성됨

## 🚨 문제 해결

### 라벨이 존재하지 않음

**문제:** `could not add label: 'task' not found`

**해결:** 스크립트가 자동으로 라벨을 생성하도록 수정됨. 최신 버전의 스크립트를 사용하세요.

### 인증 오류

**문제:** `You are not logged into any GitHub hosts`

**해결:**
```bash
gh auth login
```

### 권한 오류

**문제:** 이슈 생성 권한이 없음

**해결:** 저장소 관리자에게 권한 요청 또는 저장소 설정에서 권한 확인

## 📊 실제 실행 결과

**날짜:** 2024년

**생성된 이슈:**
- 총 15개 이슈 생성
- 모든 우선순위별 작업 포함
- 모든 이슈에 'Issue Automation' 라벨 자동 추가

**생성된 라벨:**
- task
- high-priority, medium-priority, low-priority
- testing, error-handling, accessibility
- refactoring, state-management, performance
- api, database, firebase
- search, filter, enhancement
- typescript, reports, dashboard
- Issue Automation

## 🔄 향후 개선 사항

1. **마일스톤 자동 할당**
   - 우선순위에 따라 마일스톤 자동 할당

2. **할당자 자동 지정**
   - 작업 유형에 따라 자동으로 담당자 지정

3. **프로젝트 보드 연동**
   - 이슈 생성 시 프로젝트 보드에 자동 추가

4. **일정 예약**
   - 예상 시간을 기반으로 마일스톤 날짜 자동 계산

## 📚 참고 자료

- [GitHub CLI 문서](https://cli.github.com/manual/)
- [GitHub Issues API](https://docs.github.com/en/rest/issues/issues)
- [스크립트 사용 가이드](../scripts/README.md)

---

**마지막 업데이트:** 2024년  
**작성자:** AI Assistant

